1. 先完整梳理业务，**确保已经完全理解需求了，避免返工**
	- 对功能点进行**分析**，确定界面基本构成，重点是提前明确**哪些需要进行组件封装**（先不用提前封装，但是心里要有数）
	- 如果有技术难点，先去调研。用什么库，先做个demo等
    - **先完成功能，在调整UI**
    - 搜索字段属性名从page返回值取得
    - 用到的组件，进行组件的封装
2. 将接口文档中，所有该模块涉及的 api，一次性定义在`apis`目录中
3. 在`mock`目录下对请求进行拦截、模拟（尽量真实）
    - 定义该功能涉及到的`model`**（先把接口的返回的数据的model定义出来）**
4. 将该功能涉及的所有前端枚举，一次性定义在`config/enum.ts`文件中
5. 编写模块对应的`router`
6. 编写业务代码
7. 添加国际化
    - 路由title
    - 页面
8. 添加v-auth
9. 接口联调
    - 如果接口质量不高，可以要求服务端开发人员提供他们自测通过的`postman curl`文件
10. 视觉走差：让UI和产品看看
11. ok的话，可以提测